let partyLocation = "Garden" //string; declared 'let' since the location can change

let shiftValue = 3 //number; 'const' since the number is constant throughout

let encodedMessage = ""; //string; 'let' because message will change when when solved

let decodedMessage = ""; //string; 'let' because message will change to show secret location

let isPartySecure = false; //boolean; 'let'because it may or may not change 


const shiftValue = 3;
Number.isInteger(3);